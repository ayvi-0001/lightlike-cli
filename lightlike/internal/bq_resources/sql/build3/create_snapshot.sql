CREATE OR REPLACE PROCEDURE
  ${DATASET.NAME}.${__name__}(IN name STRING)
BEGIN

EXECUTE IMMEDIATE
  FORMAT(
    """
CREATE SNAPSHOT TABLE
  ${DATASET.NAME}.%s
CLONE
  ${DATASET.NAME}.${TABLES.TIMESHEET}
    """,
  name
);

END;
